@model IEnumerable<PR_Top_Service_MVC.Models.Service>

@{
    ViewData["Title"] = "Mis Servicios";
}

<h1>Mis Servicios</h1>


<table class="table">
    <thead>
        <tr>
            <th>Descripcion</th>
            <th>Fecha</th>
            <th>Fecha de Inicio</th>
            
            <th hidden>
                @Html.DisplayNameFor(model => model.Description)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.Status_Service)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.DateTime_On_Service)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.DateTime_Off_Service)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.Latitude)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.Longitude)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.IdAdminNavigation)
            </th>
            <th hidden>
                @Html.DisplayNameFor(model => model.IdProfessionalNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
               
                <td>
                    @Html.DisplayFor(modelItem => item.Description)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Date)
                </td>
                <td hidden>
                    @Html.DisplayFor(modelItem => item.Status)
                </td>
                <td hidden>
                    @Html.DisplayFor(modelItem => item.Status_Service)
                </td>
                <td >
                    @Html.DisplayFor(modelItem => item.DateTime_On_Service)
                </td>
                <td hidden>
                    @Html.DisplayFor(modelItem => item.DateTime_Off_Service)
                </td>
                <td hidden>
                    @Html.DisplayFor(modelItem => item.Latitude)
                </td>
                <td hidden>
                    @Html.DisplayFor(modelItem => item.Longitude)
                </td>
                <td hidden>
                    @Html.DisplayFor(modelItem => item.IdAdminNavigation.IdAdmin)
                </td>
                <td hidden>
                    @Html.DisplayFor(modelItem => item.IdProfessionalNavigation.IdProfesional)
                </td>
                @if (item.Status_Service==null)
                {
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.IdService">Empezar</a>

                    </td>
                }
                else if (item.Status_Service == 1)
                {
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.IdService">Terminar</a>
                        <a asp-action="Create" asp-controller="Images" asp-route-id="@item.IdService">Subir Comprobantes</a>
                    </td>

                }
            </tr>
        }
    </tbody>
</table>
<script src="~/lib/leaflet/leaflet.js"></script>
<script src="~/lib/leaflet/leaflet.min.js"></script>

<div id="map" style="width: 100%; height: 400px;"></div>

<script>

    function iniciarMap() {
        var coord = { lat: 40.7128, lng: -74.0060 };
        var map = new google.maps.Map(document.getElementById('map'), {
            zoom: 10,
            center: coord
        });



        var jsModel = @Html.Raw(Json.Serialize(Model));

        console.log(jsModel); // Muestra los datos del modelo en la consola para verificar

        for (var i = 0; i < jsModel.length; i++) {
            var coord = { lat: jsModel[i].latitude, lng: jsModel[i].longitude };
            var marker = new google.maps.Marker({
                position: coord,
                map: map
            });
        }

    }
</script>
<script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBDaeWicvigtP9xPv919E-RNoxfvC-Hqik&callback=iniciarMap"></script>
<br />
<br />
<br />
<br />
<br />
<br />

